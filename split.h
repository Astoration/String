#include<string.h>
#include<queue>
char** split(const char* string, const char datum);
//단일 문자용 구분문자 절단함수
//2차원 배열로 0번째부터 순서대로 반환
char** split(const char* string, const char* datum);
//문자열용 구분문자 절단함수
//2차원 배열로 0번째부터 순서대로 반환